[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "llm-wiki"
version = "0.1.0"
description = "llm-wiki vault CLI, MCP server, and plugin assets"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
llm-wiki = "llm_wiki_cli:main"

[tool.setuptools]
py-modules = ["llm_wiki_cli"]
