__pycache__/
*.pyc
.venv/
.DS_Store
.tmp/
# Local dev / scratch scripts (not part of the published plugin)
scripts/.tmp/
.pytest_cache/
# QA automation: raw stream-json recordings (large)
tests/fixtures/recordings/
.cursor/
# Never keep a .claude/ directory inside this plugin repo: Claude Code skips plugin
# skills/commands when any .claude/ exists here (anthropics/claude-code#44120). Use
# ~/.claude/settings.json or ~/.claude/settings.local.json for personal env instead.
.claude/
.venv-*/
# Local vault (created by `llm-wiki setup`); tracked by its own git repo
/llm-wiki/
# Local secrets — never commit
.env.local
.env
# Scratchpad — release plans, personal notes (not part of the published plugin)
scratchpad/
.playwright-mcp/
.tmp/
.cursor/
# MCP server sidecar files (created in user vaults)
.search.sqlite3
.kg.json
.chromadb/
.metrics.jsonl
