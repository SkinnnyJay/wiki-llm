---
description: Vault-scoped git status (requires git.enabled in llm-wiki/config.json).
---

Run:

```bash
llm-wiki git status
```

If git is disabled in config, tell the user to set `git.enabled` to true.
