---
description: Show vault git diff (working tree or staged).
---

Run:

```bash
llm-wiki git diff
```

Staged only:

```bash
llm-wiki git diff --staged
```

Only works when `git.enabled` is true and `.git` exists in the vault.

$ARGUMENTS
