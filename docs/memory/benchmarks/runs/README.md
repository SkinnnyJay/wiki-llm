# Per-run Markdown reports (optional)

You can drop milestone summaries here (e.g. `lme-2026-04-09.md`) with metadata, scores, and deltas vs the previous run. The CLI records metrics in `.metrics.jsonl` and optional `metrics/runs.jsonl`; this folder is for human-readable narratives when you want them in git.
